name: Henrycat
description: 你的课表，优雅掌控
publish_to: none
version: 2.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  sqflite: 2.4.1
  path: ^1.8.3
  shared_preferences: ^2.2.2
  share_plus: ^10.0.0
  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
